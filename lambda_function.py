import boto3
import json

def lambda_handler(event, context):

    # get the bucket name from event

    # get the config file from event object key

    # call the key

    # call the process method

    return{
        "statuscode": 200,
        "body": json.dumps("Working")
    }